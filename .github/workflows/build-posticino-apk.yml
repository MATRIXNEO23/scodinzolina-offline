name: Build Posticino APK
on:
  workflow_dispatch:
  push:
    paths:
      - 'posticino-apk/**'
      - '.github/workflows/build-posticino-apk.yml'
jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: posticino-apk
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
      - name: Build debug APK
        run: gradle assembleDebug
      - uses: actions/upload-artifact@v4
        with:
          name: posticino-gptina-apk
          path: posticino-apk/app/build/outputs/apk/debug/app-debug.apk
